# Agent Usage

Contoh alur agent:
1. qmx query "quarterly planning process" --json
2. qmx multi-get "notes/*.md" --json
3. compose context ke prompt LLM

Gunakan --min-score untuk filter hasil yang relevan.
